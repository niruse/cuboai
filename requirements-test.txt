# Test dependencies for CuboAI
pytest>=9.1.1
pytest-cov>=4.0.0
pytest-asyncio>=1.4.0
aioresponses>=0.7.0

# Dependencies from the integration (needed for imports)
aiofiles>=23.0.0
aiohttp>=3.14.3,<3.15.0
boto3>=1.43.39
pycognito>=2024.5.1
PyYAML>=6.0.3
av>=18.1.0
# config_flow.py imports it; test_duplicate_account.py drives the real flow.
# It happens to be present in a local HA checkout, so a missing entry here only
# shows up in CI.
voluptuous>=0.13.1
